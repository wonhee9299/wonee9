# 인스타 shrah84 댓글 답글 + 스토리 게시

- 요청일: 2026-08-19
- 요청자 지시 원문: "인스타에 올려", "pc로 스토리까지.올려"

## 목표

릴스에 달린 shrah84 질문 댓글에 답글을 게시하고, 근거 화면을 스토리로 올린다.

## 대상

- 계정: `wonhee929` (사용자 본인)
- 릴스: https://www.instagram.com/wonhee929/reel/DcM0pGiPsN-/
- 대상 댓글 작성자: `shrah84`
- 대상 댓글: "운전습관을 fsd에 학습시킬수는 없을까요? ㅋㅋㅋ"
- 스토리 이미지: 이 저장소의 `docs/reels/assets/2026-08-19_story_shrah84.jpg`
  (1080x1920, EXIF 제거, 목적지 상호 블러 완료 — 클라우드에서 이미 게이트 통과)

## 그대로 쓸 값 — 임의 수정 금지

답글 전문:

```
개인 습관을 바로 학습하진 않더라고요. 대신 해제할 때마다 "어떤 상황이었나요?" 하고 익명으로 물어봅니다 ㅋㅋ 음성으로 상황 남기면 그게 쌓여서 다음 업데이트에 반영되는 구조라, 제 우회전 취향이 반영되려면 저 같은 사람이 더 많아야겠네요 😅 방금 스토리에 그 화면 올려놨습니다
```

문안 근거: 차량 화면 팝업이 "어시스티드 드라이빙이 해제되었습니다. 어떤 상황이었나요? /
Tesla에 내 경험을 설명하는 **익명 메시지**를 보내려면 음성 버튼을 누르세요." 이다.
"익명"은 화면 문구에 실제로 있는 표현이다.

## 실행 절차

### 1. 답글

`tools/ig_web_reply.py` 가 이 저장소에 있다.

**사용자 크롬에 인스타 로그인이 이미 되어 있다 (2026-08-19 확인).**
새로 로그인하지 말고 그 세션을 그대로 쓴다. 크롬을 원격 디버깅으로 띄우고 붙는 방식이
가장 확실하다 (크롬을 닫을 필요 없음).

```
pip install playwright && playwright install chromium

# 크롬을 원격 디버깅 포트로 띄운다 (이미 떠 있으면 완전 종료 후 다시)
#   Windows: chrome.exe --remote-debugging-port=9222
python tools/ig_web_reply.py --cdp http://localhost:9222 \
    --url <릴스URL> --reply-to shrah84 --message-file <답글파일> --dry-run
python tools/ig_web_reply.py --cdp http://localhost:9222 \
    --url <릴스URL> --reply-to shrah84 --message-file <답글파일>
```

CDP 가 안 되면 대안 순서:
1. `--chrome-user-data-dir <크롬 프로필 경로>` (크롬 완전 종료 후 실행)
2. `--login` 으로 전용 프로필에 사용자가 직접 로그인
3. 스크립트가 계속 실패하면 **브라우저를 직접 조작해서 게시한다.** 결과만 맞으면 된다

스크립트가 셀렉터 문제로 실패하면 `.ig-shots/` 스크린샷을 보고 고치거나
브라우저를 직접 조작해 게시한다. 어느 쪽이든 결과만 맞으면 된다.

### 2. 스토리

instagram.com 웹에서 Create(+) → 스토리 → 위 이미지 업로드.

아래를 **실제로 시도해보고 무엇이 되는지 기록**한다. 클라우드 쪽에서 확정하지 못한 부분이다.

1. 텍스트 도구로 `@shrah84` 입력 → 탭 가능한 멘션으로 변환되는지
2. 스티커 메뉴에 멘션 스티커가 있는지
3. 링크 스티커로 릴스 연결이 되는지
4. 웹에서 안 되면 Meta Business Suite(business.facebook.com) 에서도 시도
   (계정은 프로페셔널로 전환되어 있다)

멘션이 어떤 경로로도 안 되면 **스토리는 그대로 게시**한다. 이미지에 `@shrah84` 가
글자로 박혀 있어 누구에게 하는 말인지는 보인다. 알림은 답글이 보내므로 목적은 달성된다.

## 검증

- [ ] 답글이 **shrah84 댓글 하위**에 달렸는지 릴스 페이지에서 확인.
      새 댓글로 달렸으면 삭제하고 답글로 다시 단다 (새 댓글은 상대에게 알림이 안 감)
- [ ] 스토리가 프로필에 올라갔는지 확인
- [ ] 스토리 이미지에서 목적지 상호가 판독 불가한지 **확대해서 눈으로** 확인

## 하지 말 것

- 답글 문안 수정
- `dailes_77` 의 "ㅈㄴ듣기싫네" 댓글에 대응 (의도적 무대응)
- 스토리 이미지를 다시 만들기 (이미 게이트 통과한 파일이다)

## 사용자 승인이 필요한 지점

없다. 2026-08-19 결정으로 게시까지 자율 진행이다 (`docs/AGENT_POLICY.md`).
단 게시 전 자체 게이트는 통과해야 한다. 이미지는 이미 통과했고, 답글은 사실관계 검수 완료.
