# 🔥 반자동 핫딜 추적 시스템 사용 가이드

## 개요
- **목표**: 새 제품의 40% 이상 할인 상품을 자동으로 추출
- **방식**: 반자동 (코드가 필터링 → 사용자가 가격 확인)
- **범위**: 모든 플랫폼 (일반 포럼 + 커뮤니티)
- **종목**: 등산, 러닝, 수영, 골프, 자전거, 트레일러닝, 놀이동산, 키즈카페

---

## 📋 작동 흐름

```
1️⃣ dealscan.py 실행
   ↓
   모든 포럼 + 커뮤니티 수집 (7일 기준)
   ↓
2️⃣ hotdeal_tracker.py 실행
   ↓
   - 중고/이양 제외
   - 30,000원 이상 상품만 추출
   - 새로운 상품 감지
   ↓
3️⃣ 결과 확인
   ↓
   CSV/JSON으로 저장
   ↓
4️⃣ 사용자: 가격 확인 후 구매
```

---

## 🚀 실행 방법

### 방법 1: 한 번에 실행
```bash
# 1단계: 핫딜 수집
python3 dealscan.py

# 2단계: 필터링 및 추적
python3 hotdeal_tracker.py
```

### 방법 2: 자동 실행 (매일)
```bash
# crontab 설정 (매일 새벽 8시 실행)
0 8 * * * cd /home/user/wonee9 && python3 dealscan.py && python3 hotdeal_tracker.py
```

### 방법 3: 수동 추적
```bash
# 새 상품만 보기
python3 hotdeal_tracker.py
```

---

## 📊 결과 파일

| 파일 | 형식 | 용도 |
|------|------|------|
| `dealscan.json` | JSON | 원본 핫딜 데이터 (카테고리별) |
| `hotdeals_filtered.json` | JSON | 필터링된 상품 (30,000원 이상) |
| `hotdeal_tracker.csv` | CSV | 추적 기록 (날짜별) |

---

## 💡 필터링 규칙

### ✅ 포함 조건
- 새 제품 (암시적으로 포함됨 - 이미 핫딜 데이터)
- 가격 정보 있음 (원 단위)
- 30,000원 이상

### ❌ 제외 조건
- "중고" "이양" "팜" 키워드 포함
- "판매합니다" "구매합니다" "찾습니다" 키워드

---

## 📱 각 플랫폼 특징

| 플랫폼 | 특징 | 추적도 |
|--------|------|-------|
| 뽐뿌 | 가장 많은 핫딜 (18개) | ⭐⭐⭐⭐⭐ |
| 딜바다 | 정가 정보 있음 (5개) | ⭐⭐⭐⭐ |
| 빠삭 | 제한 기간 정보 (5개) | ⭐⭐⭐ |
| 아카라이브 | 다양한 상품 (3개) | ⭐⭐⭐ |
| 클리앙 | 전문가 추천 (1개) | ⭐⭐ |

---

## 🎯 각 종목별 최고 추적 대상

| 종목 | 상품 수 | 최고가 | 추천 상품 |
|------|---------|-------|---------|
| 등산 | 15개 | 501,270원 | 캠핑 팬히터, 등산 부츠 |
| 러닝 | 8개 | 79,190원 | 러닝화, 운동복 |
| 골프 | 2개 | 98,400원 | 골프복, 거리측정기 |
| 자전거 | 3개 | 미표시 | 라이트, GPS 컴퓨터 |
| 놀이동산 | 4개 | 360,000원 | 에버랜드, 롯데월드 이용권 |
| 수영 | 0개 | - | - |
| 트레일러닝 | 0개 | - | - |
| 키즈카페 | 0개 | - | - |

---

## 💰 "40% 이상 할인" 확인 방법

### 문제점
코드만으로는 할인율을 정확히 계산할 수 없습니다. (원래 가격 정보 부족)

### 해결책
**추천 가격 데이터베이스 구축**

```python
# wished_prices.json (추가 생성)
{
  "블랙야크 바람막이": 100000,  # 원래 가격
  "아디다스 러닝화": 150000,
  "골마켓 드라이버": 300000
}
```

그러면 자동으로 할인율 계산 가능:
```
가격: 19,900원
정가: 100,000원
할인율: 80% ✅ (40% 이상 조건 만족)
```

---

## 🔔 알림 설정 (선택사항)

### 이메일 알림 추가
```python
# hotdeal_tracker.py에 추가
import smtplib
from email.mime.text import MIMEText

# 새로운 상품이 있으면 이메일 발송
if new_items:
    send_email(new_items)
```

### 디스코드 알림 추가
```python
# Webhook으로 디스코드 메시지 발송
import requests

webhook_url = "YOUR_DISCORD_WEBHOOK_URL"
requests.post(webhook_url, json={"content": f"새 핫딜 {len(new_items)}개!"})
```

---

## 📈 데이터 분석

### CSV에서 통계 보기
```bash
# 가장 비싼 상품 Top 10
sort -t',' -k2 -nr hotdeal_tracker.csv | head -10

# 포럼별 상품 수
cut -d',' -f4 hotdeal_tracker.csv | sort | uniq -c

# 종목별 추적 현황
grep "등산" hotdeal_tracker.csv | wc -l  # 등산 상품 수
```

---

## ⚠️ 주의사항

1. **가격은 참고용**: 변동 가능성 높음
2. **링크 확인**: 게시글이 삭제될 수 있음
3. **중복 확인**: 같은 상품이 여러 포럼에 나올 수 있음
4. **수동 확인 필수**: 자동 필터링이 100% 정확하지 않음

---

## 🛠️ 커스터마이징

### 필터 기준 변경
```python
# hotdeal_tracker.py 상단에서 수정
PRICE_THRESHOLD = 50000  # 50,000원 이상으로 변경
```

### 제외 키워드 추가
```python
EXCLUDE_KEYWORDS = r'중고|이양|팜|직구|해외직배송'
```

### 특정 종목만 추적
```python
# main() 함수에서 필터링
if cat not in ['등산', '러닝']:
    continue
```

---

## 📞 자주 묻는 질문

**Q: 왜 수영/키즈카페는 상품이 없나요?**
- A: 일반 포럼에서는 거래가 거의 없습니다. 커뮤니티 중심으로 움직입니다.

**Q: 정가를 어디서 알 수 있나요?**
- A: G마켓, 쿠팡, 공식 사이트에서 검색하면 "정가" 또는 "원래가격" 표시됨

**Q: 매일 실행해야 하나요?**
- A: 매일 1회 실행 권장 (새 상품 감지를 위해)

**Q: 커뮤니티 핫딜은 왜 없나요?**
- A: 커뮤니티는 별도 스크래핑 필요 (회원 인증, 동의 문제)

---

## 📝 로그 확인

```bash
# 실행 기록 저장
python3 dealscan.py 2>&1 | tee logs/dealscan_$(date +%Y%m%d).log
python3 hotdeal_tracker.py 2>&1 | tee logs/tracker_$(date +%Y%m%d).log

# 어제 실행 결과 보기
cat logs/tracker_20260921.log
```

---

**마지막 업데이트**: 2026-09-22  
**다음 실행 권장**: 매일 아침 8시
