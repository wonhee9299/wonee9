# 릴스3 — 테슬라 모델3/FSD 실사용 릴스 프로젝트

이 저장소는 인스타그램 릴스 제작 프로젝트 "릴스3"의 작업 컨텍스트를 보관한다.
새 세션을 시작하면 이 문서와 `docs/reels/` 를 먼저 읽고, 아래 내용은 **확정 인수인계**로 취급한다.
이미 확정된 사항은 사용자에게 다시 묻지 않는다.

## 계정 · 대상

| 항목 | 값 |
|---|---|
| Instagram | `wonhee929` |
| 콘셉트 | 오래 탄 테슬라 오너의 실사용 기록 |
| 차량 | 한국 2022 테슬라 모델3 롱레인지 리프레시, Intel Atom 기반 |
| 기준 주행거리 | 약 103,340km |

## 개인정보 원칙

얼굴 / 개인 위치 / 번호판 / 지도 / GPS / VIN 은 **원칙적으로 보호(마스킹)** 한다.
단, 사용자가 명시적으로 허용한 항목(예: "테슬라 지도는 마스킹 안 해도 돼")은
**그 작업 범위 안에서만** 예외로 따른다. 다음 작업으로 예외를 확대하지 않는다.

## 실행 위치 (중요)

인스타 게시, 원본 영상 편집, NAS 접근은 **PC의 Remote Control 세션에서만** 가능하다.
클라우드 세션은 매번 빈 컨테이너라 로그인된 브라우저도 원본 파일도 없다.

폰에서 작업할 때는 두 가지 방식이 있다.

1. **PC RC 세션을 직접 골라 명령** — 실시간으로 주고받을 때. `docs/reels/REMOTE_CONTROL_SETUP.md`
2. **클라우드 세션에 명령하고 PC로 위임** — 던져놓고 결과를 나중에 확인할 때.
   `docs/reels/DISPATCH.md`

### 위임받은 작업을 처리할 때 (PC 세션)

트리거로 작업을 받으면 수행 후 **결과를 `handoff/results/` 에 기록하고 커밋·푸시한다.**
트리거는 단방향이라 이게 클라우드 세션에 결과를 알리는 유일한 방법이다.
실패해도 반드시 남긴다. 형식은 `docs/reels/DISPATCH.md` 참고.

## 작업 방식

- 말만 하지 말고 **직접 실행한다.** 파일 확인, 영상 분석, 편집, 업로드까지 수행한다.
- **인스타 최종 게시/공유 버튼은 사용자가 "올려" / "게시"라고 명확히 말했을 때만** 누른다.
- 게시 후에는 **실제 URL, 프로필 그리드 커버, 첫 댓글**까지 반드시 확인한다.
- 판단이 틀리면 변명하지 말고 **원인 → 재확인 → 수정** 순으로 처리한다.
- 보고는 **짧고 빠르게.** 장황한 설명 금지.
- 사용자가 실망하거나 화낼 수 있다. 방어적으로 굴지 말고 즉시 수정안을 내고 실행한다.

## 영상 제작 전 필독 (workspace 규칙)

로컬 `ai-video-studio/` 의 아래 3개 문서를 **영상 제작 착수 전 반드시 읽는다.**

1. `ai-video-studio/VIDEO_PRODUCTION_MASTER_RULES.md`
2. `ai-video-studio/MANDATORY_VIDEO_PRODUCTION_CHARTER.md`
3. `ai-video-studio/INSTAGRAM_REELS_PRODUCTION_GATE.md`

> 이 저장소에는 아직 원본이 없다. 반입 방법은 `docs/reels/RULES_IMPORT.md` 참고.

## 릴스 제작 기준

- **세로 릴스 폼 우선.**
- 자동차 주행영상은 화면을 무리하게 자르지 말고 **원본 구도를 살리되, 위아래 자막 공간을 적극 활용**한다.
- 자막은 **크고 읽기 쉽게.** 나레이션과 자막 타이밍이 **반드시** 맞아야 한다.
- 정지화면을 늘어놓지 말고 **실제 주행 영상 중심**으로 구성한다.
- BGM은 사용자가 원하면 넣고, 원하지 않으면 뺀다. **최근 선호: BGM 빼고 남자 나레이션.**
- 음성: **Airy Studio**, 남자 목소리는 **Zippy** 를 지정한 이력이 있다.

### 사용자가 중요하게 보는 포인트

- FSD가 잘한 구간
- 답답해서 운전자가 개입한 구간
- 우회전 / 좌회전 / 차선변경 / 합류
- **"위험"과 "운전습관상 답답함"을 구분해서** 표현할 것

### 훅 · 구성 취향

- 훅이 약하면 안 된다. **첫 화면에서 넘기지 않게 하는 제목/자막이 가장 중요.**
- 조회수 높은 영상의 구조에서 영감을 얻되, **허위 과장은 하지 않는다.**
- 잘 먹힌 구조: **비용·숫자·의문 → 실제 화면 → 직접 확인 → 결론·질문**

## 새 작업 시작 절차

작업 순서는 `docs/reels/WORKFLOW.md` 의 7단계를 그대로 따른다.
게시 이력은 `docs/reels/EPISODES.md` 에 기록하고, 게시할 때마다 갱신한다.
