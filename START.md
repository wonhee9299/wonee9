# 시작하기 — PC에서 한 줄, 그 다음은 폰으로

코덱스처럼 폰에서 시키려면, PC에서 클로드를 한 번만 켜두면 됩니다.

## 1단계 — 클로드가 PC에 없다면 설치
공식 페이지에서 설치: https://claude.com/product/claude-code
(이미 설치되어 있으면 건너뜀)

## 2단계 — 원격 조종 켜기
**윈도우 키** → `powershell` 입력 → 엔터 → 파란 창에 아래 입력:

```
claude remote-control
```

로그인 창이 나오면 평소 계정으로 로그인.

## 3단계 — 폰 연결
화면에 **QR 코드**가 뜨면 폰의 Claude 앱으로 찍기.
(또는 폰 Claude 앱 세션 목록에서 PC 세션 선택)

## 끝
그 다음부터는 폰에서 말만 하면 PC의 클로드가 실행합니다.

> "코덱스로 뭐 했었는지 찾아줘" (.codex 폴더에 기록이 남아 있음)
> "나스 연결해줘" — 나스 코드는 이 저장소 `infra/` 에 준비되어 있음

막히면 화면을 찍어 클로드 대화에 붙여넣으세요.
